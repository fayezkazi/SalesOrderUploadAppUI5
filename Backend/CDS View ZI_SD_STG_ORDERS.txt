@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Order Upload Staging – Interface View'
@Metadata.ignorePropagatedAnnotations: true
@ObjectModel.usageType: {
  serviceQuality: #X,
  sizeCategory:   #S,
  dataClass:      #TRANSACTIONAL
}
define root view entity ZI_SD_STG_ORDERS
  as select from zsd_stg_orders
{

      //"-------- Key -----------------------------------------------------------
  key stg_guid        as StgGuid,

      //"-------- Header --------------------------------------------------------
      auart           as Auart,
      vkorg           as Vkorg,
      vtweg           as Vtweg,
      spart           as Spart,
      vkbur           as Vkbur,
      kunnr_ag        as KunnrAg,
      kunnr_we        as KunnrWe,
      augru           as Augru,
      bstnk           as Bstnk,
      bstdk           as Bstdk,
      vdatu           as Vdatu,

      //"-------- Address / Partner ---------------------------------------------
      name1           as Name1,
      name2           as Name2,
      stras           as Stras,
      str_suppl       as StrSuppl,
      ort01           as Ort01,
      regio           as Regio,
      pstlz           as Pstlz,
      smtp_addr       as SmtpAddr,

      //"-------- Item ----------------------------------------------------------
      posnr           as Posnr,
      matnr           as Matnr,
      kwmeng          as Kwmeng,
      vrkme           as Vrkme,
      charg           as Charg,
      werks           as Werks,

      //"-------- Staging Control -----------------------------------------------
      status          as Status,
      status_msg      as StatusMsg,

      //"-------- Admin ---------------------------------------------------------
      created_at      as CreatedAt,
      created_by      as CreatedBy,
      last_changed_at as LastChangedAt,
      last_changed_by as LastChangedBy
}

managed implementation in class zbp_sd_stg_orders unique;
strict ( 2 );

define behavior for ZI_SD_STG_ORDERS alias Orders
persistent table zsd_stg_orders
lock master
authorization master ( instance )
etag master LastChangedAt
{
  create;
  update;
  delete;

  static action excel_upload parameter ZA_SD_ORDERS_UPLOAD_ACTION
    result [0..*] ZA_SD_ORDERS_UPLOAD_RESULTS;

  static action staging_data parameter ZA_SD_ORDERS_UPLOAD_ACTION result [0..*] ZA_SD_ORDERS_UPLOAD_RESULTS;

  static action process_staging parameter ZA_SD_ORDERS_UPLOAD_ACTION result [0..*] ZA_SD_ORDERS_UPLOAD_RESULTS;

  static action delete_staging result [0..*] ZA_SD_ORDERS_UPLOAD_RESULTS;

  // ── Field control ─────────────────────────────────────────────────────────
  // UUID is generated by the managed framework
  field ( numbering : managed, readonly ) StgGuid;

  // Admin / audit fields are set by the framework and determination
  field ( readonly ) //Status, StatusMsg,
                     CreatedAt, CreatedBy,
  LastChangedAt, LastChangedBy;

  // Mandatory fields – framework enforces presence before save
  field ( mandatory ) Auart, Vkorg, Matnr;

  // ── Validations (fired before each save) ──────────────────────────────────
  validation validateMandatory on save { create; }
  validation validateSalesOrg on save { create; }

  // ── Determinations (fired before each save) ───────────────────────────────
  determination setInitialStatus on save { create; }
  determination setAdminData on save { create; }


  determination updateAdminData on save { field Auart, Vkorg, Vtweg, Spart, Vkbur,
  KunnrAg, KunnrWe, Augru, Bstnk, Bstdk, Vdatu, Name1, Name2, Stras,
  StrSuppl, Ort01, Regio, Land1, Pstlz, SmtpAddr, EdiHdrText, Posnr, Matnr, Kwmeng,
  Vrkme, Charg, Werks, Status, StatusMsg, Vbeln; }

  // ── Field mapping : CDS element name → database table column ──────────────
  mapping for zsd_stg_orders corresponding
  {
    StgGuid = stg_guid;
    Auart = auart;
    Vkorg = vkorg;
    Vtweg = vtweg;
    Spart = spart;
    Vkbur = vkbur;
    KunnrAg = kunnr_ag;
    KunnrWe = kunnr_we;
    Augru = augru;
    Bstnk = bstnk;
    Bstdk = bstdk;
    Vdatu = vdatu;
    Name1 = name1;
    Name2 = name2;
    Stras = stras;
    StrSuppl = str_suppl;
    Ort01 = ort01;
    Regio = regio;
    Land1 = land1;
    Pstlz = pstlz;
    SmtpAddr = smtp_addr;
    EdiHdrText = edi_hdr_text;
    Posnr = posnr;
    Matnr = matnr;
    Kwmeng = kwmeng;
    Vrkme = vrkme;
    Charg = charg;
    Werks = werks;
    Status = status;
    StatusMsg = status_msg;
    Vbeln = vbeln;
    CreatedAt = created_at;
    CreatedBy = created_by;
    LastChangedAt = last_changed_at;
    LastChangedBy = last_changed_by;
  }
}